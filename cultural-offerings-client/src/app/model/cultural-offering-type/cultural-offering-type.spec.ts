import { CulturalOfferingType } from './cultural-offering-type';

describe('CulturalOfferingType', () => {
  it('should create an instance', () => {
    expect(new CulturalOfferingType()).toBeTruthy();
  });
});
