import { SimpleSnackbarProps } from './simple-snackbar-props';

describe('SimpleSnackbarProps', () => {
  it('should create an instance', () => {
    expect(new SimpleSnackbarProps()).toBeTruthy();
  });
});
